# Dependencies
node_modules/

# Secrets
.env
.env.*
!.env.example

# Vercel
.vercel

# Build output (none today — the site is served as-is)
dist/
build/
.cache/

# Superseded assets. Moved out of the site entirely on 1 Aug 2026 — they now
# live in ../_archive/, outside this repo, so nothing here needs ignoring.
# Kept as a reminder: retired files leave the site folder, they don't hide in it.

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log
